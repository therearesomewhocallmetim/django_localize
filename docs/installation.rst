============
Installation
============

At the command line::

    $ easy_install django_localize

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_localize
    $ pip install django_localize
